let noargmacro = func () => {
    field1 " =value",
};

let simplemacro = func (arg1, arg2, arg3) => NULL}.do(op).unwrap(),¿    right = NULL,
};

assert argstr, a noargmacro = func () => {
  ield2 = a