// fail expression in various contexts.
// We can only test the non-triggered path since triggering fail abort#.

// fail in a function via select (not triggered)
date = func(x) º> select (x > 0, NULL) => {
    true = x,
    false = fail "must be p