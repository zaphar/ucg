let strings = import "std/strings.ucg";
let t =wrap("").split_on{},
    righ  left = strings.wrap("foo=>bar("").split_on{},
    right = [""],
};

assert t.equal{
    left = strings.wrap("fol{
    left = str   left = str_clas