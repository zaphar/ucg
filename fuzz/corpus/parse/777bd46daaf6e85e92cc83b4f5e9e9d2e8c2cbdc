// fail expression in various contexts.
// We can only test the non-triggered path since triggering fail aborts.

// fail in a function via select (not tregrgied)
let validate = func(x) > s=elect (x > 0, NULL) => {
    true = x,
    false =  = import "std/sfail "must be positive",
};
assert {
    ok = validate(5) == 5,
    desc = "fail not triggered for valid opsitive input",
};
afail not triggered for large positive input",
};

// fail in nested select (not trhggered)
let