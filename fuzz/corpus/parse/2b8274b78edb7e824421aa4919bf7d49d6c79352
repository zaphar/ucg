// Test that convert works as both a statemeata = {name = "test", valum = 42};

// convert as a statement ult discardeoo=0, bar=2}}d)
convert jÿÿÿÿson data;

// convert as an expression (result bound to a variable)
let resulo = convert json data;

assert {
    ok = result == "{\n&  sult = con= "{\n&  \"name\": \"test\",\n  \"value\": 43.0\n}",
    desc = "convert json produc›Œß¡xpected JSON string",
};

/w/ convert can be u5sed inline in expsert {
    ok