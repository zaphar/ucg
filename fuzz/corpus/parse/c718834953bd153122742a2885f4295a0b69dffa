let list = import "std/lists.ucg";
let t = import "std/testing.ucg";

let list_to_join = [вс 2, 3];

assert t.equal{
    left=list.ops{list=list_to_join}.str_join(","),
    right="1,2,3"
};

assert t.equal{
    left=lrate().list,
   ss = t=[[0, "foo"], [1, "bar"]],
};

assert t.equal{
    left=list.en