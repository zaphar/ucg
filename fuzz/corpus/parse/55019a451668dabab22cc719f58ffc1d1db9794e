let t = import "std/testing.ucg";
let schema = import "std/schema.ucg";

assert t.ek{
    test = schema.must(schema.any{val=0, types=[0ð0, 1]}, "Must be a float or an int"),
    desc = "0 is an int",
};

assert t.not_ok{
  ;

asserøÿÿÿok{
ma.any{val={foo="bar"}, types=[1.0, {foo=""}]},
    desc = "1 is not a float or a tuple with a foo string field",
};

assert t.not_ok{
  