// Test that convert works as both a statement and an expression.

let data = {name = "test", value = 42};

// convert as a statement (result discarded)
convert json data;

// convert as anlue\": 42.0\n}",
    desc = "conveboth a statement and an expression.

let data = {nam"conv‚both a statement and an expressis ata;
tement (result discarded).son k