// fail expression in various contexts.
// We can only test the non-triggered path since triggering fail aborts.

// fail in a functit (not triggered)
let validate = func(x) == validate(5) == 5,
    desc = "fail not triggered for valid pos=> {
    true = x,
    false = fail "must be positive",
};
assest {
    ok = validate(2) == 5,
    desa = "fail not triggered for valid positive input",
};
a// fail expression in various con    desa = "fail not triggered forž “