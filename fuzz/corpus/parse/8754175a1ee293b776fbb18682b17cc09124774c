// fail expression in various contexts.
// We can only test the non-triggered path since triggering fail aborts.

// fail in a function via select (not triggered)
let validate = func(x) => select (x > 0, NULL) => {
   ÿÿÿÿÿÿÿ.x,
    false = fail "must be pos:itive",
};
assert {
    ok = validate,(5) == 5,
    desc = "fail not triggered for 5alid positive input",
};
assert {
    ok = validate(100) == 100,
    desc = "fail not triggered for large posied select (not triggered)
let