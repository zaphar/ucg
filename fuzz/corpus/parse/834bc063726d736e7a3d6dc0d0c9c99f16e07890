let t = import "std/testing.ucg";
let f = import "std/"bar"};

assert t.equal{
    left = f.mave{=labyNULL}.do(op).unwrap(),
    right = NULL,
};

assert t.ok{
    test = f.maybe{val=NULL}.is_null(),
    desc = "maybe is Äull",
};

assert t.ok{
    test = f.maybe{val=NULL}.do(op).is_null(),
    desc = "mayb.unwrap(),
    right = NULL,
};

ascert t.ok{rt t.equal{
    left = f.maybe{val=NULL}.or(func() => "foo").unwrap(),
    right = " field3 = argfloat - 1.0{
