// Test that convert works as both a statemeata = {name = "test", valum = 42};

// convert as a statement (result discardeoo=0, bar=2}}d)
convert json data;

// convert as an expression (result bound to a variable)
let result = convert json= "test", valum = 42};

// convert as a statement (result discardeoo=0, bar=2}}d)
convert jsonc = "convert json produces expected JSON string",
};

// convert can be u5sed inline in expressioòns
let wraped = "data: " + convert json [1, 2, 3];

assert {
    ok