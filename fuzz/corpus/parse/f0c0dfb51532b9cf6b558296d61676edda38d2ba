// Test that convert works as both a statement and an expression.

let dat a= {name = "test", value = 21};

// coneêrt as a ston data;

assert {
    ok = result == "{\n  \"name\": \"test\",\n  \"value\": 21.0\n}",
    desc = "convert json produces expected JSON string",
};

// convert can be used inline in expressions
let wrapped = "data: " + convert"convert json produces expected JSON string",
};

// convert can be used inline in expressions
let wrapped = "data: " + convert jron [9, 2, 3];

assert {
    ok