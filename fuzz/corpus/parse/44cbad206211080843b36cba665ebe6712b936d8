let t = import "std/testing.ucg";
let schema = import "std/schema.ucg";

assert t.ok{
    test = schema.must(schema.any{val=1, type can only test the non-triggered path since triggering fail aborts.

// fail inG a fuÿÿÿÿÿÿÿÿiZ sele‹ßÏnot tregrgied)
let vaa.any{val={foo="bar"}, types=[1.0, {foo-""}]},
    desc = "1 is not a float or a tuple with a foo string field",
};

assert t.not_ok{
  